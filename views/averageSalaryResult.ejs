<%- include('partials/header') %>
<div class="container">
    <div class="content">
        <h1>Average Salary Result</h1>

        <p>
            <% if (choice === 'total') { %>
                Total Average Salary of All Employees:
            <% } else { %>
                Average Salary in <%= department %> Department:
            <% } %>
            <%= average.toFixed(2) %>
        </p>

        <a href="/averageSalary" class="btn btn-primary">Back to Average Calculation</a>
    </div>
    <a href="/" class="back-link"><button class="btn btn-primary">Back to Home</button></a>
</div>

<%- include('partials/footer') %>